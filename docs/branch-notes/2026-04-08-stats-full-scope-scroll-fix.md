# 2026-04-08 — Stats full-scope visibility and scrolling fix

## Summary
- Updated the Transcript Metrics grouping label from `Scope` to `Full Scope` so the section matches operator wording.
- Updated the Stats panel container layout CSS to fully occupy the right-rail slot (`height: 100%`) and use an explicit column flex layout so the panel body can scroll reliably when content exceeds available height.

## Validation
- Ran frontend lint and test commands from the repository root.

## What Did Not Work
- No failed implementation attempts during this change.
