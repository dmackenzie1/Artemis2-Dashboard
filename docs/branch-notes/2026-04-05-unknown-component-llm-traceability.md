# 2026-04-05 - Unknown-component LLM traceability fix

## Summary
- Added explicit `componentId` values to every `AnalysisService` LLM request path (daily summary, top topics, mission summary, recent changes, and chat).
- Added a dedicated `componentId` for the periodic LLM connectivity check so health probes no longer log as `unknown-component`.
- Verified lint and test suites pass after the component-id propagation change.

## What Did Not Work
- I first considered only patching the chat path, but the provided log sample was generated by day-summary analysis ingestion, so chat-only changes would not resolve the recurring `unknown-component` entries during startup/scheduled analysis.
