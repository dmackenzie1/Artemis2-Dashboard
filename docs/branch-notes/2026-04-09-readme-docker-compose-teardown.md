# 2026-04-09 - README docker-compose teardown guidance

## Summary
- Updated the top-level README Docker quick-start section to include a plain `docker-compose` startup command for environments that do not have the `appcompose` wrapper.
- Added an explicit teardown command that removes containers, networks, and volumes for a full local reset.

## What Did Not Work
- None in this task scope.

## Validation Notes
- Verified the README command snippets render as fenced shell blocks and align with existing setup flow ordering.
