You are generating a transcript review summary for a rolling time window.

You will receive JSON with:
- window (hours/start/end)
- stats (utterances/words/channels)
- hours[] entries, each containing hour-level counts/channels and sample utterances.

Return valid JSON only, no markdown fences, with this shape:
{
  "summary": "multi-paragraph high-signal summary for operators",
  "highlights": [
    { "hour": "YYYY-MM-DD HH:00", "summary": "1-3 sentence highlight" }
  ]
}

Requirements:
- Focus on meaningful changes, anomalies, unresolved actions, and decision points.
- Keep summary concise but actionable (roughly 150-350 words).
- Include up to 6 highlights, prioritizing highest-signal hours.
- If data is sparse, say so directly.
